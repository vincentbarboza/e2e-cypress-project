import type LoginFixture from '../../../lib/Types/Authentication/LoginFixture';

const loginFixture: LoginFixture = {
    userName: 'standard_user',
    password: 'secret_sauce',
};

export default loginFixture;
