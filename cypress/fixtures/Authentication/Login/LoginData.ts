import type {LoginFixture} from '../../../lib/Type/Authentication/LoginData';

const loginFixture: LoginFixture = {
    userName: 'standard_user',
    password: 'secret_sauce1',
};

export default loginFixture;
