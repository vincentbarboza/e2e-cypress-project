name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality-and-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Lint
        id: lint
        run: npm run eslint-cypress

      - name: Typecheck
        id: typecheck
        run: npm run typecheck

      - name: Cypress run
        uses: cypress-io/github-action@v6
        with:
          command: npm run cypress:run
        env:
          CI: true

      - name: Fail workflow if lint/typecheck failed
        if: ${{ steps.lint.outcome == 'failure' || steps.typecheck.outcome == 'failure' }}
        run: exit 1          